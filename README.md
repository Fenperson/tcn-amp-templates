# Generated AMP deployment artifact

This branch is generated from the per-template source directories on `main`.
Its intentionally flat root layout is required by AMP Application Deployment.

Do not edit template behavior on this branch. Make changes under
`templates/<application>/` on `main`, validate them, generate a fresh artifact,
and publish the reviewed result here.
